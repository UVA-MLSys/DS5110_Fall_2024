import pytest

# test model doesn't degrade


def test_model_exists() -> None:
    # check that the model exists in directory
    assert False


def test_model_accuracy() -> None:
    # check that the model is making good predictions
    assert False